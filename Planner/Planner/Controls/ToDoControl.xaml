<UserControl x:Class="Planner.Controls.ToDoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Planner.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
<<<<<<< Updated upstream
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        
        <!-- TODO list -->
        <ListBox Margin="10" Name="CheckboxList" Grid.Row="0" SelectionChanged="OnItemSelected" SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                    
                        <CheckBox Grid.Column="0" Margin="0 0 5 0"></CheckBox>
                        <Grid Grid.Column="1">
                            <TextBox DataContext="bbb" AcceptsReturn="True" Loaded="FocusOnLoad" Focusable="True" TextWrapping="NoWrap" Name="TodoTextInput"
                                     BorderThickness="0" Background="Transparent"/>    
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <!-- Bottom footer -->
        <DockPanel Grid.Row="1">
            <Button Name="AddNewTaskButton" Content="Add" DockPanel.Dock="Right" Width="100"/>
            <TextBox Name="NewTaskInput" Margin="0 0 5 0"/>
        </DockPanel>
    </Grid>
=======
    <DockPanel>        

        <ListView Margin="10" Name="todoListView" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel Height="20" Background="Red" HorizontalAlignment="Stretch" LastChildFill="True">
                        <CheckBox Name="Checkbox"  VerticalAlignment="Center" Margin="0,0,5,0" DockPanel.Dock="Left"/>
                        <TextBox Name="Text" />
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </DockPanel>
>>>>>>> Stashed changes
</UserControl>
